export default function RestrictUpdate(props) {

    // Worst Practice to follow. 
    // props.cloneData.name = "sdfkugsdg"

    debugger;

    return (
        <h2>User Name is: {props.cloneData.name}</h2>
    )
}